#Operating System: Works on majority of Linux OSes
#Misc Notes: Possibly recreate the script to take a username to reset instead of one user. If so, rename to genericResetUserPasswd.

#wonder how to handle piping in input for password entry
sudo passwd root
